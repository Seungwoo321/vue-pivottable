import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs/blocks'

import PivottableUi from '../PivottableUi'
import { data } from './utils/data'

<Meta title="PivottableUi" component={PivottableUi} />

# PivottableUi

<ArgsTable of={PivottableUi} />

<Canvas>
  <Story name="With data">
    {{
      components: { PivottableUi },
      template: `<pivottable-ui
            :data="data"
            :selected-aggregators="[
              { name: 'Sum', vals: ['ReservationRemaining'] },
              { name: 'Sum', vals: ['AvailableToSell'] }
            ]"
            :rows="['PC9']"
            :cols="['Gender']"
          />`,
      data() {
        return {
          data
        }
      }
    }}
  </Story>
</Canvas>
