const toArray = (value) => Array.isArray(value) ? value : value ? [value] : [];
export const getContributorInfo = (contributorName, infos = []) => infos.find(({ username, alias }) => username === contributorName || toArray(alias).includes(contributorName)) ?? null;
