import type { KnownGitProvider } from '../../shared/index.js';
export declare const getRemoteUrl: (cwd: string) => string;
export declare const inferGitProvider: (cwd: string) => KnownGitProvider | null;
