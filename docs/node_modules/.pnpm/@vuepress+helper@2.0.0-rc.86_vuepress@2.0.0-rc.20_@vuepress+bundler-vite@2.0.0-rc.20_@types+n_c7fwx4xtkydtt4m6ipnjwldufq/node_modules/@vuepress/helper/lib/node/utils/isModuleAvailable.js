/**
 * Check if a module is available
 */
export const isModuleAvailable = (module, meta) => {
    try {
        if (module)
            meta.resolve(module);
        return true;
    }
    catch {
        return false;
    }
};
